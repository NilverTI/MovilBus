{
  "name": "movilbus-app",
  "version": "1.0.0",
  "description": "Sistema de gestión MovilBus PeruServer",
  "private": true,
  "scripts": {
    "proxy": "node proxy/server.js",
    "dev": "node --watch proxy/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}